@page
@model ClientsModel
@{
    // Redirect handled in page model — keep a minimal view so the route still resolves.
    ViewData["Title"] = "Clients";
}
<p class="text-secondary">Redirecting to Client Version…</p>
