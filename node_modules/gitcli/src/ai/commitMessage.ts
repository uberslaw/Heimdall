import { AIStyle, generateAIResponse } from './provider.js';

export async function generateCommitMessage(diff: string, style: AIStyle = 'conventional') {
  const prompt = `Generate a short git commit message in ${style} style based on this diff:\n${diff}`;
  return generateAIResponse(prompt, style);
}
