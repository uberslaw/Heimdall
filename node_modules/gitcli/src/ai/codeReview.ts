import { generateAIResponse } from './provider.js';

export async function reviewDiff(diff: string) {
  const prompt = `Review the following staged diff and provide concise, actionable feedback:\n${diff}`;
  return generateAIResponse(prompt);
}
