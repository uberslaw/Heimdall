import { generateAIResponse } from './provider.js';

export async function summarizeCommits(log: string) {
  const prompt = `Summarize the following commits into a PR title and description:\n${log}`;
  return generateAIResponse(prompt);
}
