import { generateAIResponse } from './provider.js';

export async function resolveConflict(file: string, content: string) {
  const prompt = `Resolve merge conflicts in ${file}. Return merged content only.\n${content}`;
  return generateAIResponse(prompt);
}
