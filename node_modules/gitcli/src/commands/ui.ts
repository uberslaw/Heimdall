import { Command } from 'commander';
import { showDashboard } from '../tui/dashboard.js';

export function registerUI(program: Command) {
  program
    .command('ui')
    .description('Interactive TUI dashboard')
    .action(async () => {
      await showDashboard();
    });
}
